type Query {
  helloWorld: String!
  product(slug: String!): Product
}

type Product {
  productId: String!
  productName: String!
  metaTagDescription: String!
  categoryNames: [String!]
  firstItem: Item
}

type Item {
  itemId: String
  nameComplete: String
  price: Number
  imageUrl: String
}
